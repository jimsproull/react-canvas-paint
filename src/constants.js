const PENCIL = 'pencil';
const BRUSH = 'brush';
const ERASER = 'eraser';
export const BRUSHES = {
    PENCIL,
    BRUSH,
    ERASER
};

const CLEAR = 'clear';
const DRAW = 'draw';
export const MODES = {
    CLEAR,
    DRAW
};
