const PENCIL = 'pencil';
const BRUSH = 'brush';
const ERASER = 'eraser';
export const BRUSHES = {
    PENCIL,
    BRUSH,
    ERASER
};

const CLEAR = 'clear';
const DRAW = 'draw';
const UNDO = 'undo';
const REDO = 'redo';
export const MODES = {
    CLEAR,
    DRAW,
    UNDO,
    REDO
};
